package dev.l2j.autobots.ui.tabs

internal enum class BotDetailsTab {
    Info,
    Social,
    Skills,
    Combat,
    Chat
}

internal enum class BotDetailsCombatEditAction {
    None,
    TargetRadius,
    KiteRadius
}

internal enum class BotDetailsChatTab {
    All,
    Pms
}

internal enum class BotDetailsSocialPage {
    Home,
    CreateSellStore,
    CreateBuyStore,
    CreateCraftStore
}